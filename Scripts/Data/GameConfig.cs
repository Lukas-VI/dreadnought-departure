namespace DreadnoughtDeparture.Core;

/// <summary>
/// 全局静态配置常量。
/// 所有与六角格尺寸、物理参数相关的硬编码值集中在此，便于统一调参。
/// </summary>
public static class GameConfig
{
	/// <summary>六角格外接圆半径（世界单位）。</summary>
	public const float HexRadius = 2.0f;
}
